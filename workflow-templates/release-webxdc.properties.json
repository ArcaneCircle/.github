{
  "name": "Create .xdc Release",
  "description": "On each tag in the form `v*.*.*`, build a release of your project with `pnpm build` and create a github release with the `.xdc` file in the `dist` folder",
  "filePatterns": [
    "index.html",
    "manifest.toml",
    "package.json"
  ]
}
